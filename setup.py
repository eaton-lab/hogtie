#! usr/bin/env python

from setuptools import setup

setup(
    name="hogtie",
    version="0.0.1",
    packages=[],
    entry_points={
        'console_scripts': ['hogtie = hogtie.__main__:main']
        }
)
